int main() {
   /*this 
   isa block*/

  for(int i = 0; i < 10 && i > 0; i++) {
   print(i);
  }
  for(int i = 0; i <= 10  || i >= 0; --i, i++) {
    do{
      if(x == 20) {
        printf("this code is not supposed to make sense and won't compile");
        char_literal = 'A';
      }
    }while(x++);
  }
  //this is a line comment;
}